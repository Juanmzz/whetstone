{"id": "sig-0001", "ts": "2026-07-08T15:40:00-03:00", "type": "build-before-validate", "phase": "plan", "severity": "medium", "detail": "While planning V0, nearly committed to building the automated retro loop (v1) before dogfooding it by hand on a real project — build ahead of validation. Corrected: Wizard-of-Oz the loop manually first, then automate only if it produces value."}
{"id": "sig-0002", "ts": "2026-07-12T16:36:01-03:00", "type": "emitter-duplication", "phase": "init", "severity": "medium", "detail": "On the invoice-sync-engine dogfood, init.md's emitter (phase 4) wrote CLAUDE.md and AGENTS.md with byte-identical content (confirmed via diff -q). Duplication violates token-economy T1, and Claude Code reads ONLY CLAUDE.md while AGENTS.md is the cross-vendor standard. Fixed: AGENTS.md canonical + CLAUDE.md = one-line @AGENTS.md import.", "rule_affected": ["skills/token-economy.md"]}
{"id": "sig-0003", "ts": "2026-07-14T03:26:45-03:00", "type": "unverified-api-assumption", "phase": "apply", "severity": "medium", "detail": "Built the strict-path-guard hook using permissionDecision 'defer' from an unverified claude-code-guide answer. 'defer' is headless-only and wrong for an interactive passive warning (correct: exit 0 + additionalContext, no permissionDecision). Caught by the crossed adversarial review + a grounding pass. Lesson: ground every API claim against the docs before shipping code — even a claim from a sub-agent. voice V2 (verify before asserting) applies to sub-agent output too.", "rule_affected": ["skills/xreview.md", "skills/voice.md"], "resolved_by": "skills/xreview.md@v2"}
{"id": "sig-0004", "ts": "2026-08-07T22:20:00-03:00", "type": "unverified-flag-assumption", "phase": "apply", "severity": "high", "detail": "The build plan specified `claude -p --output-format stream-json` for the llm/ adapter without ever running it. Live probing found three errors: stream-json errors without --verbose; a naive invocation inherits the caller's whole ambient config (MCP servers, plugins, SessionStart hooks) costing 140,682 input tokens / $0.84 for a one-word answer versus ~11.4k / $0.03-0.08 hermetic — a 10x cost gap that would have silently destroyed the frugality thesis; and --bare, the obvious fix, forces ANTHROPIC_API_KEY and never reads OAuth, which would lose the subscription cost advantage. Validating risk #1 at Step 0 instead of at integration time was correct. Same class as sig-0003.", "rule_affected": ["skills/xreview.md", "skills/voice.md"], "resolved_by": "skills/xreview.md@v2"}
{"id": "sig-0005", "ts": "2026-08-07T22:25:00-03:00", "type": "schema-valid-but-garbage", "phase": "verify", "severity": "high", "detail": "Using --system-prompt (replacing rather than appending) made the model leak raw tool-call markup (</parameter>, </invoke>) INTO a schema-valid string field. Native --json-schema validation passed on a payload that was unusable. Empirical proof that the LLM verdict contract needs contamination detection plus validate-and-retry, not just schema validation — the design assumed schema validation would be sufficient. Fixed in core/llm/verdict.ts with the real payload as a regression fixture; --append-system-prompt is now mandatory.", "rule_affected": ["skills/tdd-discipline.md"]}
{"id": "sig-0006", "ts": "2026-08-07T22:45:00-03:00", "type": "overeager-guard", "phase": "apply", "severity": "medium", "detail": "The first contamination check matched a bare '</', which would have flagged any agent-lens legitimately quoting closing tags while reviewing HTML or JSX ('the </div> is unclosed') and turned every front-end review into a retry loop that fails closed. Caught by writing the false-positive test before trusting the guard. Lesson: a guard against garbage needs a test proving it accepts the legitimate case, not only that it rejects the bad one.", "rule_affected": ["skills/tdd-discipline.md"], "resolved_by": "skills/tdd-discipline.md@v3"}
{"id": "sig-0007", "ts": "2026-08-07T23:05:00-03:00", "type": "calibration-passed", "phase": "verify", "severity": "low", "detail": "ADR-0008's pre-registered kill criterion was measured at Step 0: a correctness lens returned 10/10 correct and unanimous on BOTH a known-good and a known-bad fixture (claude 2.1.225, sonnet, $0.79 for 20 calls). The agent-lens-can-earn-block assumption survives its first test. CAVEAT, recorded so it is not overclaimed: the fixtures are mirror-image and unambiguous (removing vs adding a null check before a dereference). This proves the harness works and the easy case is stable; it does NOT prove stability on borderline diffs, which is where a gate actually lives. Harder fixtures are owed before any lens ships as severity: block.", "rule_affected": []}
{"id": "sig-0008", "ts": "2026-08-08T00:20:00-03:00", "type": "guard-too-blunt", "phase": "verify", "severity": "high", "detail": "sig-0005's contamination rule rejected ANY tool-call markup in a verdict. Measured over 80 calibration runs, the artifact is a well-formed TRAILING suffix (the model closes its tool call inside the string) with complete, correct prose in front of it — and it is size-correlated: 0/40 runs on diffs under 10 lines, 13/40 on 11-15 line diffs. Rejecting the whole verdict discarded CORRECT answers and burned 3 billed retries into the same deterministic failure, leaving the gate blind on ~33% of realistic-length diffs. Fixed: strip the trailing artifact and report it via `sanitized`; markup embedded mid-content still fails closed. Re-measured live: 0/8 blind, down from 33%. Lesson: a guard written from a 2-sample observation was calibrated to the wrong failure shape — the rule was right in kind and wrong in degree.", "rule_affected": ["skills/tdd-discipline.md", "skills/xreview.md"], "resolved_by": "skills/tdd-discipline.md@v3"}
{"id": "sig-0009", "ts": "2026-08-08T00:25:00-03:00", "type": "overclaimed-from-small-sample", "phase": "verify", "severity": "medium", "detail": "`.sdd/architecture.md` recorded '--append-system-prompt clean 2/2' as evidence the contamination was fixed. That was 2 runs on a one-line prompt. At realistic diff lengths the same contamination recurs on a third of calls (sig-0008). The claim was not wrong so much as measured on an input unlike the real workload. Lesson: an evidence table is only as good as the representativeness of its inputs — record N and the input size next to any stability claim, not just the ratio.", "rule_affected": ["skills/voice.md"], "resolved_by": "skills/voice.md@v2"}
{"id": "sig-0010", "ts": "2026-08-08T00:30:00-03:00", "type": "filtered-run-reads-as-global", "phase": "verify", "severity": "medium", "detail": "`npm run calibrate -- --filter race` printed 'PASS — this lens may be declared severity: block' after measuring only 2 of 10 fixtures. A subset run that emits the global promotion verdict is how a lens gets promoted on the fixtures it happens to be good at — the precise result-fitting ADR-0008 pre-registers against. Fixed: a filtered run now prints PARTIAL and states it cannot promote. Lesson: any tool that can run on a subset must say so in its verdict, not only in its invocation.", "rule_affected": []}
{"id": "sig-0011", "ts": "2026-08-08T01:00:00-03:00", "type": "lens-prompt-shapes-verdict", "phase": "verify", "severity": "medium", "detail": "Changing an agent-lens prompt from 'does this introduce a bug?' to 'name a concrete failing input under the change's stated contract, else pass' removed nearly all false positives (nullish-good 8/10 -> 5/5, boundary-good 7/10 -> 5/5) WITHOUT introducing false negatives (all five known-bad fixtures stayed 5/5). The obvious risk of raising the burden of proof for `fail` — starting to miss real bugs — did not materialise at N=5. Lesson: a flaky agent-lens is often a badly-specified one, not an unstable one; rewrite the lens before concluding the check cannot be calibrated. Still FAILS the bar: race-good flips 1/5.", "rule_affected": []}
{"id": "sig-0012", "ts": "2026-08-08T01:05:00-03:00", "type": "duplicated-source-of-truth", "phase": "verify", "severity": "high", "detail": "scripts/calibrate.ts kept its own copy of the review lens with a comment saying 'must stay in sync with .sdd/checks/correctness.md'. That is a receipt-integrity hole: on drift you calibrate one prompt and ship another, and the check's recorded `calibration:` block vouches for text that never ran — a receipt that certifies the wrong artifact. Fixed: the harness now loads the lens through the check registry. Lesson: anything that produces a RECEIPT must read the artifact it certifies, never a copy of it. Same class as ADR-0006's warning that a version string is not identity.", "rule_affected": ["skills/tdd-discipline.md"]}
{"id": "sig-0013", "ts": "2026-08-08T02:10:00-03:00", "type": "broken-tool-read-as-failure", "phase": "verify", "severity": "high", "detail": "A deterministic check runs through a shell, so a MISSING BINARY is not a spawn error — the shell starts fine and exits 127. Read naively that is a non-zero exit, i.e. a failing check, so one missing tool would have BLOCKED every change in the repo and blamed the change for it. The gate lane found this by running the gate on this repo and now maps 126/127 to `errored`, checked BEFORE the exit code because a broken run usually also reports non-zero. Verified with npm and claude off PATH: two block-severity checks fully broken, verdict pass, blocking empty, exit 2. Lesson: 'the tool failed' and 'the code failed' arrive on the same channel (exit status) and must be separated deliberately, or a gate blames the author for its own breakage.", "rule_affected": []}
{"id": "sig-0014", "ts": "2026-08-08T02:15:00-03:00", "type": "invalid-verification", "phase": "verify", "severity": "medium", "detail": "While verifying the gate blocks on a real failure, the mutation I applied was a NO-OP: I pattern-matched `status: \"renamed\"` but the source has `status: letter === \"R\" ? \"renamed\" : \"copied\"`, so perl replaced nothing. The gate correctly returned pass, and I nearly recorded that as 'the gate fails to block'. Caught by checking whether the mutation actually landed AND whether the tests actually went red before trusting the gate's answer. Lesson: a negative test must first prove it broke what it claims to have broken — otherwise you are measuring your own patch, not the system.", "rule_affected": ["skills/tdd-discipline.md", "skills/xreview.md"], "resolved_by": "skills/tdd-discipline.md@v3"}
{"id": "sig-0015", "ts": "2026-08-08T03:10:00-03:00", "type": "gate-verified-nothing", "phase": "verify", "severity": "high", "detail": "The first `wst run` gated with --range HEAD, which means working-tree-vs-HEAD. The crewmate COMMITS its work, so by gate time the working tree is clean and the diff is EMPTY: the gate matched no files, applied no checks, and correctly reported 'nothing about this change was verified' — while `wst run` printed PASSED above it. The gate's honesty is the only reason this was caught; a gate that reported a bare 'pass' for an empty diff would have hidden it. Fixed: capture the base commit before dispatch and gate `base..HEAD`, plus refuse outright when the crewmate committed nothing. Lesson: 'no checks ran' and 'all checks passed' are different results and must never share a success message.", "rule_affected": [], "resolved_by": "skills/delegation.md@v2"}
{"id": "sig-0016", "ts": "2026-08-08T03:15:00-03:00", "type": "misdiagnosed-from-post-mortem-state", "phase": "verify", "severity": "medium", "detail": "Diagnosing the above, I inspected the worktree AFTER the run had already released it. `treehouse return --force` resets the worktree to the pool base, so the tree I examined had no src/ and no package.json — and I announced the bug was 'far worse', that the crewmate had branched from the wrong commit and worked on an empty tree. Wrong: the branch ref survived, and it held exactly the right four-file commit. The evidence I was reading was destroyed by the cleanup I had just run. Lesson: when a run cleans up after itself, diagnose from the artifacts that SURVIVE cleanup (branch refs, logs) — and state which state you are looking at before drawing a conclusion from it.", "rule_affected": ["skills/xreview.md", "skills/voice.md"], "resolved_by": "skills/xreview.md@v2"}
{"id": "sig-0017", "ts": "2026-08-08T04:00:00-03:00", "type": "asked-to-judge-unseen-artifact", "phase": "retro", "severity": "high", "detail": "The first real `wst retro` returned 3 of 4 proposals as the literal word 'placeholder', one explaining 'I don't have visibility into .sdd/skills/voice.md'. Cause: the proposer runs HERMETIC (no tools, no filesystem, by design — it is a judge), so it was asked to amend a rule whose text it had never been shown. Fixed by putting the target skill's current content and the list of real skills into the prompt; the second run produced 5 substantive proposals citing real signals. Lesson: a hermetic judge can only reason about what is IN the prompt — every artifact it must judge has to be passed in, and 'it has tools' is a property of crewmates, not of judges.", "rule_affected": ["skills/delegation.md"], "resolved_by": "skills/delegation.md@v3"}
{"id": "sig-0018", "ts": "2026-08-08T04:05:00-03:00", "type": "gate-forwarded-a-non-answer", "phase": "retro", "severity": "medium", "detail": "The anti-poisoning gate checked that a rationale was non-EMPTY but not that it was substantive, so proposals whose entire body was 'placeholder' passed validation and reached the human. The model had honestly signalled it could not do the job and the gate treated that as a valid proposal. Fixed: reject placeholder/TBD markers and rationales under 60 chars. Lesson: a validator that only checks for absence will forward a non-answer — 'the model said something' is not the same as 'the model answered'.", "rule_affected": ["skills/xreview.md"], "resolved_by": "skills/xreview.md@v3"}
{"id": "sig-0019", "ts": "2026-08-08T04:40:00-03:00", "type": "spec-itself-violates-its-adr", "phase": "init", "severity": "medium", "detail": "Porting docs/woz/init.md to code surfaced that the WoZ SPEC ITSELF violates ADR-0004: its AGENTS.md template tells a bootstrapped repo to 'run the retro (see retro.md)' — a Whetstone document that does not exist in the target, so the reference dangles the moment the payload travels. Same bug class as the skills/recording.md citation fixed earlier, but this one was in the procedure rather than in an artifact, so every repo the procedure bootstrapped would have inherited it. The init lane described the retro inline instead. Lesson: a self-containment rule has to be enforced against the GENERATOR, not only audited on its output — the init code now runs a reference-closure check that refuses to emit a plan mentioning a path the plan does not create.", "rule_affected": []}
{"id": "sig-0020", "ts": "2026-08-08T05:00:00-03:00", "type": "payload-lookup-escapes-package", "phase": "init", "severity": "high", "detail": "package.json had `files: [\"dist\"]`, so `.sdd/skills/` — the payload init copies into a target — was never published. Fixing that exposed a worse bug in findPayloadRoot(): it walked up from the module unbounded, so installed at <target>/node_modules/whetstone/dist/commands/ it would escape the package and find <target>/.sdd/skills — THE TARGET'S OWN SKILLS. init would copy a project's skills back onto itself and report success, indistinguishable from a correct bootstrap. Fixed: the walk stops at the package.json declaring name===whetstone and never crosses a node_modules boundary. Verified from a simulated install with a planted impostor skill. Lesson: a 'walk up until you find X' lookup has no notion of ownership — in a package that ships inside someone else's tree it will happily find THEIR X.", "rule_affected": [], "resolved_by": "skills/xreview.md@v3"}
{"id": "sig-0021", "ts": "2026-08-08T05:05:00-03:00", "type": "broken-harness-read-as-result", "phase": "verify", "severity": "medium", "detail": "Verifying the payload-boundary fix, the test reported 'impostor survived' three times running — and all three were the HARNESS failing, not the code: first the simulated install lacked node_modules so the CLI died with ERR_MODULE_NOT_FOUND, then it died again on the same cause, then on an invalid --risk flag I had passed myself. Each time the impostor file was untouched simply because init never executed, which looks identical to init running and picking the wrong payload. Caught by asserting the mechanism RAN (wst.yaml exists) before reading the result — which is exactly xreview XR5 as amended by retro-0016, applied within the hour of writing it. Lesson: a negative result from a harness that never executed is not a result; assert execution before interpreting outcome.", "rule_affected": ["skills/xreview.md"]}
{"id": "sig-0022", "ts": "2026-08-08T05:40:00-03:00", "type": "error-detail-lost-at-adapter", "phase": "verify", "severity": "high", "detail": "The claude CLI exits NON-ZERO on its own terminal errors (notably error_max_budget_usd) while still writing a complete parseable envelope to stdout. execFile rejects on that exit code, and shell/claude.ts discarded stdout on rejection, so every budget exhaustion surfaced as kind 'spawn' (could not run the check) instead of 'budget' (ran out of money). classifyHardError already had a budget branch; it was simply unreachable on this path. The gate works hard to keep 'the tool broke' separate from 'the check failed', and the distinction was being destroyed one layer below it. Fixed: parse stdout on rejection when it is a complete envelope, rethrow only when it is not. Lesson: an adapter that drops output on a non-zero exit silently downgrades every structured error the tool took the trouble to report.", "rule_affected": []}
{"id": "sig-0023", "ts": "2026-08-08T05:45:00-03:00", "type": "lens-never-runs-on-real-diffs", "phase": "verify", "severity": "high", "detail": "The correctness lens is systematically not running on realistic strict-tier changes. route() sends strict to opus, and the gate sends ONE call containing the entire matched diff under a fixed 0.50 USD ceiling. Measured on a real 114 KB diff: the call cost 0.607 USD and was killed by the cap, so the lens errored. Consequence lands on Layer 5: every strict PR renders NOT VERIFIED: correctness could not run. The gate is honest about it, which is why it was visible at all, but the differentiator is inert in practice. Candidate fixes: per-file or chunked lens calls, a tier-scaled budget, or both. Not yet fixed.", "rule_affected": [], "resolved_by": "core/gate/chunk.ts"}
{"id": "sig-0024", "ts": "2026-08-08T06:30:00-03:00", "type": "hypothesis-refuted-by-measurement", "phase": "verify", "severity": "medium", "detail": "I claimed the 549-test suite was inflated by crewmates optimising for thoroughness, and proposed cutting tests in the lane-written annotate/ and checks/ files. Mutation testing refuted it. Score 34/40 (85%) killed; the 9.2-tests-per-mutation figure I called redundancy is mostly CROSS-FILE integration coupling, which is legitimate: a mutation in findings.ts kills tests in annotate.test.ts and body.test.ts because those exercise the pipeline. Reading the two largest crewmate files found no duplication either — 34 distinct behaviours in body.test.ts, 16 distinct properties in criticality.test.ts. Nothing was deleted. What the measurement DID produce was two real gaps (a block/advisory filter flip and a truncation off-by-one) that survived the whole suite, now closed and verified to kill their mutants. Lesson: 'this feels like too many tests' is a hypothesis, and mutation testing is cheap enough to test it before acting on it — the instrument found gaps, not bloat.", "rule_affected": ["skills/tdd-discipline.md", "skills/voice.md"]}
{"id": "sig-0025", "ts": "2026-08-08T07:00:00-03:00", "type": "reset-destroyed-real-work", "phase": "verify", "severity": "high", "detail": "To prove the new pre-push hook blocks a bad push, I broke a test, ran `git add -A && git commit` and pushed. The commit swept up the hook, the CI workflow, the --no-lens flag and the status changes ALONGSIDE the deliberate sabotage. Undoing the sabotage with `git reset --hard HEAD~1` therefore destroyed all of it: four files gone from the working tree, test count 568 -> 566. Recovered via `git checkout <reflog-sha> -- <paths>`, verified the sabotage did NOT come back with it. Root cause: `git add -A` while the working tree contained both real work and a deliberate defect, then treating the commit as if it held only the defect. Lesson: never stage a deliberate defect with `-A`. Commit the real work FIRST, then make the defect as a separate uncommitted change so undoing it is `git checkout -- <file>` and cannot reach anything else.", "rule_affected": ["skills/delegation.md", "skills/xreview.md"], "resolved_by": "skills/xreview.md@v3"}
{"id":"sig-0026","ts":"2026-08-08T07:40:00-03:00","type":"verification-pollutes-the-evidence","phase":"verify","severity":"medium","detail":"The first thing automatic signal emission did was record my own deliberate sabotage as genuine friction. Proving the gate blocks requires breaking something on purpose, and the engine cannot distinguish a planted defect from a real one, so the evidence log gained a fabricated entry the moment it started writing itself. Removed by hand, and --no-emit is now reachable from the CLI so verifying the gate cannot pollute the record it produces. Note the shape: this is the same failure as sig-0025 (a negative control contaminating real work) one layer up, now in the evidence rather than the working tree. Lesson: any mechanism that records what it observes needs an explicit off switch for the runs where you are testing the mechanism itself, or its first act is to poison its own input.","rule_affected":["skills/xreview.md"]}
{"id": "sig-0027", "ts": "2026-08-08T22:15:00-03:00", "type": "stub-outlived-its-stated-precondition", "phase": "verify", "severity": "high", "detail": "`wst gate` routed from `provisionalRouting`, a stub that hard-coded `strict` and never read `.sdd/triage.yaml`. Its justifying comment said `core/triage/` \"is being built in a parallel lane and does not exist yet\" — but triage SHIPPED in the same PR, and `wst pr` was already consuming it (classify + route, pr.ts:219). So the gate, which IS the enforcement channel (pre-push hook + CI), ignored the project's own triage rules, while the annotation that describes the gate used them. Two commands could route the same change differently. Measured after the fix on its own diff: classified `light` selects [test, typecheck]; the old forced `strict` also selected `correctness`, the agent-lens — so until now every full gate run billed a model even on doc-only changes. It failed SAFE (over-verified), which is exactly why nobody noticed for a whole PR. Fixed by lifting `loadRules` out of pr.ts into `shell/sdd.ts` as `loadTriageRules` and calling classify+route in gate.ts. Note the shape: this is the SAME failure as the duplicated check runner (gate.ts:169) — a second implementation of a routing decision living in a composition root, which `.sdd/triage.yaml`'s own rule for `src/commands/**` predicts verbatim: \"a decision found here is a design bug before it is a testing one\". Lesson: a stub's justification is a claim with an expiry date. When the thing it stands in for lands, the comment becomes a lie that reads like documentation, and nothing in the toolchain checks it. Found by a human-guided read of the PR, NOT by the gate — the gate cannot catch this class.", "rule_affected": ["skills/xreview.md", "skills/tdd-discipline.md"], "resolved_by": "src/commands/gate.ts@classify+route"}
{"id": "sig-0028", "ts": "2026-08-08T23:20:00-03:00", "type": "cache-key-narrower-than-what-it-vouches-for", "phase": "verify", "severity": "high", "detail": "The receipt input hash covered the changed files plus the check's declared `version`, and nothing else. `version` is hand-maintained and nothing in the schema forces a bump, so editing a check's `command` (npm test -> npm test -- --shard=1/4) left every existing receipt matching: the gate would report PASS for a check that had never run in its current form. Found by an external comparison against Trunk Check, which solves this with `affects_cache`. Note the asymmetry that makes it worse than the same bug elsewhere: a stale key in Turborepo replays an old build, a stale key here reports a clean gate on code no check examined. Fixed by hashing the behaviour-determining fields directly (command, review_lens) via a `CheckIdentity` built in ONE place (`identityOf` in gate/run.ts), so the hash the gate compares and the hash recordPass mints cannot diverge. RECEIPT_INPUT_FORMAT bumped 1 -> 2, so every receipt on disk is re-earned. Version SURVIVES as the manual override for behaviour changes the fields cannot observe. STILL OPEN and documented in place rather than hidden: the hash cannot see what the command reaches at runtime (.eslintrc, tsconfig, a pinned tool version, fixtures, env). The complete fix is an explicit `affects` path list in the check schema, which is a schema change and therefore a conversation. Lesson: a cache key must cover everything the cached claim depends on, and 'we version it by hand' is a promise, not a mechanism. Prefer hashing the thing over hashing a human's assertion about the thing.", "rule_affected": ["skills/xreview.md"], "resolved_by": "src/core/receipts/hash.ts@CheckIdentity"}
{"id": "sig-0029", "ts": "2026-08-08T23:30:00-03:00", "type": "guard-narrower-than-the-blast-radius", "phase": "init", "severity": "high", "detail": "`wst init`'s only overwrite guard covered `.sdd/`, but the plan writes AGENTS.md, CLAUDE.md, .claude/settings.json and .claude/hooks/ too, and `writePlan` is mkdir -p + writeFile with NO existence check. So a repo that had never seen Whetstone but did have hand-written versions of those four lost all of them, silently, no backup, not even a line in the output. CLAUDE.md is replaced by a ONE LINE pointer; .claude/settings.json by an object containing only Whetstone's hook, wiping permissions/env/statusLine/other hooks. Worst possible shape for a destructive bug: it fires on the FIRST command a new user runs, on the repo they are evaluating the tool with, and it takes exactly the files a careful team hand-maintains. The docstring also claimed `--force` 'prints what it is about to overwrite' and it did not, so the file asserted a safety property it did not have. Fixed with a pure `collisionsIn` over every planned path (files AND copies -- guarding only `files` would have left the whole skills payload open), each collision carrying a STAKE in the human's terms, because a bare path list does not distinguish an empty signals.jsonl coming back empty from losing a hand-written CLAUDE.md. --force now genuinely prints the list first. Verified end to end against a throwaway repo in the scratchpad, NOT this one (sig-0025/0026). Lesson: a guard's scope must be derived from what the operation writes, not from the one path that felt important when it was written. When the writer has no check of its own, the guard is the only thing between the plan and somebody's work -- so it belongs next to the plan, covering all of it.", "rule_affected": ["skills/xreview.md"], "resolved_by": "src/core/init/collisions.ts"}
{"id": "sig-0030", "ts": "2026-08-08T23:32:00-03:00", "type": "boolean-collapsed-three-states-into-destructive-advice", "phase": "verify", "severity": "high", "detail": "`wst status` modelled hooks as one boolean, `hooksInstalled`, computed IN THE COMMAND as `hooksPath === '.githooks'`. git has exactly one core.hooksPath, so husky, lefthook and Whetstone are mutually exclusive — but the boolean could not tell 'nothing configured' from 'another tool configured', so a repo on `.husky` reported identically to a repo with no hooks and got told to run `git config core.hooksPath .githooks`. Following that disarms husky. Worse in combination with the fact that `wst init` never writes a `.githooks/` directory into a target repo: the advice would disarm the existing hooks AND install nothing in their place. Fixed by replacing the boolean with `HookFacts { configuredPath, whetstoneHooksPresent }` and branching three ways: another owner -> name it and give NO command, because choosing to disarm someone's setup is not status's call; no .githooks/ to point at -> say so; otherwise -> the original advice. The render names the owner too, since a bare 'NOT active' reads as 'nothing guards this repo', the opposite of the truth on a husky repo. Note WHERE the bug lived: the comparison was made in `src/commands/status.ts`, a composition root, which is exactly what `.sdd/triage.yaml`'s rule for `src/commands/**` warns about — 'a decision found here is a design bug before it is a testing one'. That is the THIRD finding this session with that shape (sig-0027, the duplicated check runner, and now this). Lesson: a boolean that answers 'is it ours?' hides 'whose is it?', and any advice derived from it will eventually be advice to overwrite a stranger's work. Model the owner, not the ownership.", "rule_affected": ["skills/xreview.md", "skills/tdd-discipline.md"], "resolved_by": "src/core/status/report.ts@HookFacts"}
{"id": "sig-0031", "ts": "2026-08-09T01:30:00-03:00", "type": "hermetic-judge-starved-of-the-evidence-it-needed", "phase": "init", "severity": "medium", "detail": "FIRST SIGNAL EARNED FROM A LIVE RUN RATHER THAN WRITTEN BY HAND. `wst init --propose` shipped passing 16 unit tests and a prompt carrying the file list, scripts, commit subjects and detection evidence. The first real invocation returned a refusal: 'I cannot determine what this project is for from the evidence provided', and then named exactly what was missing — 'I was not shown the contents of package.json (no dependency list) or src/a.ts, which are the two things most likely to settle purpose'. The evidence-over-assertion rule worked (it refused to guess, and flagged zero risk because it could justify none), but the prompt was starving it. Nothing in the test suite could have caught this: every test asserted what the prompt CONTAINS, and the defect was what it OMITS. Fixed by inlining the README (truncated at 4000 chars) and the dependency NAMES (not versions — the pin is noise). Re-run on the same repo went from a refusal to three risk flags, each citing files, and one carrying its own falsification condition: 'INFERRED from dependencies only ... should be dropped if the bot uses long polling instead of a webhook'. Cost $0.16 -> $0.12, so more context was also cheaper than a refusal plus a retry. Also corrected a claim in the CLI hint that the call costs 'a few cents'; measured at ~$0.15. Lesson: for a hermetic judge, the prompt IS the world. Unit tests can pin what you put in it and can never tell you what you left out — only a live run can, and the model will usually say so if the prompt gives it permission to admit ignorance.", "rule_affected": ["skills/xreview.md", "skills/delegation.md"], "resolved_by": "src/core/init/propose.ts@buildProposalPrompt"}
{"id": "sig-0032", "ts": "2026-08-09T14:00:00-03:00", "type": "truncation-amputated-a-word-in-a-permanent-name", "phase": "dispatch", "severity": "low", "detail": "OBSERVED IN THE WILD, not constructed. A real `wst run` dispatch on the VPS produced the branch `run/fix-init-see-monorepos-and-their-tests-i`, where the trailing `-i` is the amputated start of `inside`. `branchNameFor` did `.slice(0, 40).replace(/-+$/, '')`, and stripping a trailing HYPHEN never removes the word STUMP a mid-word cut leaves. Low severity by any functional measure — the ref is valid and everything works — but a branch name is read by a human in `git branch`, in `gh pr list` and in every merge commit for the life of the repo, so the carelessness is permanent and free to avoid. It was noticed by the human as 'no lo veo muy prolijo' before anyone looked at the code, which is the useful part: a cosmetic defect in a name was the first thing a user reacted to about `wst run`, ahead of anything the dispatch actually does. Fixed by cutting at the last word boundary that fits, with a hard cut retained only when the first word alone exceeds the budget (a stump beats an empty branch name). Note also that the RED test I wrote asserted the wrong expected value — I predicted the cut one word earlier than it lands — and the failing test caught my error rather than the code's. Lesson: output a human will live with for years deserves the same care as output a machine parses, and 'it is only cosmetic' is the argument that lets a permanent blemish ship.", "rule_affected": ["skills/voice.md"], "resolved_by": "src/core/dispatch/charter.ts@truncateAtWord"}
{"id": "sig-0033", "ts": "2026-08-09T15:10:00-03:00", "type": "hermetic-judge-was-never-hermetic", "phase": "verify", "severity": "high", "detail": "HARD RULE 9 WAS FALSE THE WHOLE TIME. `shell/claude.ts` claimed the judge ran hermetic via --strict-mcp-config --mcp-config {} + --settings {hooks:{}} + --tools \"\". Measured against claude 2.1.226 by asking the judge to enumerate its own context: it was ALSO carrying the caller's ~/.claude/CLAUDE.md, ~/.claude/rules/*.md, a user-level SessionStart hook that fired and injected an engram memory block with prompts from unrelated projects, and the auto-memory MEMORY.md index for the cwd. --settings only overrides the PROJECT setting layer; it never touched the user layer. Found not by review but by READING A VERDICT: `wst init --propose` on sift-app wrote 'prior sessions record you also use this repo as a practice sandbox for interview prep', which is impossible for a hermetic call. Traced verbatim to a MEMORY.md line, 'uses sift-app as a tutor-mode sandbox'. Three consequences, worst last. (1) Security: the stated property that a repo cannot hijack its own reviewer held, but the CALLER's global config could, and did. (2) Cost: architecture.md documents ~11.4k input tokens as the hermetic baseline; genuinely hermetic measures 3,506. The frugality figure was measured under contamination and is 3x too high. (3) THE SERIOUS ONE: a calibration receipt is measured under whatever the measuring machine's ~/.claude/ happened to contain, so it does not transfer to another machine or another day. Calibration gating an agent-lens's right to block is this project's one differentiator, and it was resting on an uncontrolled variable. Fix is two parts: `--setting-sources \"\"` (loads no setting source at all, killing CLAUDE.md, global rules and user hooks) and running the child in `tmpdir()` instead of inheriting the target repo's cwd, because auto-memory is indexed BY DIRECTORY and no flag disables it (--bare does, but forces ANTHROPIC_API_KEY and abandons OAuth). The judge never needed a cwd: rule 9 already requires everything it judges to be inlined. Verified end to end: same prompt on the same repo, no trace of the foreign context. Lesson: a security property asserted in a comment and never probed is a hypothesis. This one was load-bearing for four other claims and was wrong for the life of the file. Probe the boundary by asking the thing on the other side what it can see.", "rule_affected": ["skills/xreview.md", "skills/tdd-discipline.md"], "resolved_by": "src/shell/claude.ts@setting-sources+neutral-cwd"}
{"id": "sig-0034", "ts": "2026-08-09T16:50:00-03:00", "type": "the-moat-was-an-honour-system-checkbox", "phase": "verify", "severity": "high", "detail": "AGENTS.md non-negotiable 2 reads 'A judgment check earns its `block` — enforced by the SCHEMA', and an outside audit had called that mechanism the one thing it could not find in any comparable tool. It was false. `CheckSchema` asked exactly one question: does the YAML say `calibration.status: passed` with `runs >= 1`? Two hand-typed fields. DEMONSTRATED rather than argued: edited three lines of .sdd/checks/correctness.md in a scratch copy (severity warn->block, status uncalibrated->passed, runs 0->1), fed it to parseCheckFile, and it loaded at `block`. A lens that had never been measured had blocking authority over every push. Found by an external agent (Codex) reading schema.ts:87 while answering a product-positioning question — not by any test, review or retro here, across the whole life of the file. Fixed with `core/calibration/receipt.ts`: authority now requires a receipt carrying sha256 of the exact lens text, sha256 of the fixture set (paths + content + EXPECTED verdict, so flipping ground truth invalidates it), model, runtime version and the raw per-run results, with `verdict` DERIVED by a single constructor and re-derived at parse so a hand-edited file disagreeing with its own results fails to parse. The old `status` field is gone from the schema entirely rather than deprecated, so a stale check file fails loudly. All six paths verified: no evidence, absent receipt, different lens, changed fixtures, failed measurement -> denied; genuine receipt -> loads. Note the shape: this is sig-0028 one level up. There, a check's `command` entered the gate receipt hash because trusting a human to bump `version` did not hold. Here the lens prompt enters the calibration hash for the same reason, and correctness.md was ALREADY carrying the warning as prose ('Changing the lens INVALIDATES the previous measurement') addressed to whoever remembered to read it. Lesson: the property you advertise as your differentiator is the one nobody audits, because everyone assumes it was the thing you got right. Write the adversarial test for your own moat first, not last.", "rule_affected": ["skills/xreview.md", "skills/tdd-discipline.md"], "resolved_by": "src/core/calibration/receipt.ts@blockAuthority"}
{"id": "sig-0035", "ts": "2026-08-09T18:10:00-03:00", "type": "honest-prose-over-a-lying-exit-code", "phase": "verify", "severity": "high", "detail": "`exitCodeFor` returned 0 for a run in which nothing was verified. The render was already correct — `report.ts`'s own header promises that 'no checks applied' is 'reported as itself rather than dressed up as a pass', and it printed exactly that — but the promise was kept one layer above the number that agents, git hooks and CI steps actually read. FOUND BY A CREWMATE, not by a test: dispatched with 'run the checks yourself', it ran `wst gate --no-lens --no-emit`, got the default `--range HEAD` against a clean tree, was told 'nothing about this change was verified', and received exit 0. It reported the contradiction in its own final message. Note what that means: the instruction in the charter was satisfiable by a gate that verified nothing, so 'the crewmate ran the checks' was not evidence of anything. Fixed: a run now exits INCOMPLETE unless at least one check passed or was skipped BY RECEIPT — a receipt skip is verification (that exact input passed already), while 'nothing matched' is not, and collapsing the two would make the cache look like a hole. Two legacy tests encoded the old behaviour, and one of them was a genuine conflict rather than an oversight: it asserted 0 for a run whose ONLY check was an errored advisory, under the correct rule that a flaky `warn` lens must not fail CI. Separated the two — the rule protects a VERIFIED run from a flaky model, it was never meant to bless a run with no verification in it. Lesson: an honest message and a dishonest exit code is worse than both being dishonest, because the message buys trust that the number then spends. Whatever is machine-read is the interface; prose is commentary on it.", "rule_affected": ["skills/xreview.md", "skills/delegation.md"], "resolved_by": "src/core/gate/report.ts@exitCodeFor"}
{"id": "sig-0036", "ts": "2026-08-09T18:25:00-03:00", "type": "the-worker-minted-the-receipts-its-own-gate-honoured", "phase": "verify", "severity": "high", "detail": "OBSERVED on the first real `wst run` dispatch. The supervising gate — the one whose entire job is to judge the crewmate independently before a human sees the work — reported `skipped (receipt)` for all three checks and passed. The receipts in the worktree were timestamped during the crewmate's own run: the worker had run the checks, earned the receipts, and the gate that came after trusted them. In this instance the outcome was correct, because deterministic checks are deterministic and the input hash genuinely matched. The MECHANISM is the defect. `.sdd/receipts/*.json` is a plain file the crewmate has write access to, and `parseReceipt` validates SHAPE, not PROVENANCE — a crewmate can compute a valid `inputHash` and write a passing receipt for a check that never ran. `charter.ts:110` tells every crewmate 'a worker that can merge its own work has no gate'; a worker that can mint the receipts its gate honours is one step short of that. Fixed with `noReceipts`, a store that reads null and writes nothing, used by `wst run` when gating inside the worktree it is judging. Exported as `createDistrustfulReceiptStore` rather than inlined, because three separate bugs today (sig-0027, the duplicated check runner, sig-0030) were one rule implemented twice and drifting. Verified behaviourally: run 1 passes and earns a receipt, run 2 skips by receipt, run 3 with the distrustful store re-runs. Lesson: a cache is a trust relationship. Ask who can write to it, and whether that party has an interest in what it says — here the subject of the audit was also its record-keeper, and content-addressing does not fix that because the content is also theirs.", "rule_affected": ["skills/xreview.md", "skills/delegation.md"], "resolved_by": "src/commands/gate.ts@createDistrustfulReceiptStore"}
{"id": "sig-0037", "ts": "2026-08-09T18:40:00-03:00", "type": "append-only-file-with-a-read-modify-write-id", "phase": "verify", "severity": "medium", "detail": "`shell/signals.ts` opens with a header arguing that the log is written with `appendFile` and NEVER read-modify-write, because 'rewriting it to add a line is how a concurrent gate run silently drops someone else's signal'. The ID allocation four functions below did exactly that: `nextId(existing)` read the whole log and returned `max + 1`. Two gates in parallel worktrees read the same log, compute the same next number, and both write it — duplicate ids in the evidence the retro clusters over. The file got the hard part right and lost it on the easy one. Not observed in the wild, because only one crewmate has ever run at a time; it would have arrived the first day of the multi-agent work that is the whole point. Fixed by deriving the id from the `fingerprint` the signal ALREADY carries for dedupe: `sig-<8 hex of sha256(fingerprint)>`. No read, so nothing to race, and two machines with different logs agree. Cost accepted: ids stop being readable in order — `sig-0034` told you it was the 34th, `sig-a3f21c9d` tells you nothing — which was never information worth a correctness bug, and the timestamp is on every line. sig-0001..0037 keep their sequential hand-written ids; ids must be unique, not ordered, and rewriting the record to match a new scheme is what the memory rules forbid. STILL OPEN and worth naming: the log is one tracked file that every branch appends to at the same line, so parallel branches still conflict on merge. Unique ids make that conflict trivially resolvable (keep both lines) instead of dangerous (two different signals claiming one id). One-file-per-signal would remove it entirely and is a format change touching retro. Lesson: a file can be append-only and still have a read-modify-write hiding in what it appends.", "rule_affected": ["skills/recording.md"], "resolved_by": "src/core/signals/emit.ts@signalId"}
{"id": "sig-0038", "ts": "2026-08-09T19:20:00-03:00", "type": "patch-applied-without-verifying-it-applied", "phase": "verify", "severity": "high", "detail": "Patched `scripts/calibrate.ts` to write the calibration receipt using a Python `str.replace` whose pattern omitted a trailing comma present in the source. `str.replace` with no match returns the string unchanged and raises nothing. I did not assert, did not re-read the file, and reported the feature as done. The user then paid for a full 100-call calibration ($3.98) whose only artifact was console output — the receipt, which is the entire point of the day's work, was never written. Some of the same session's edits DID use an assert; this one did not, so the discipline existed and was applied unevenly. Lesson: a silent-on-failure edit tool needs the assertion at the call site every time, not when it occurs to you. And 'the tests pass' proved nothing here because the changed code path is a script with no test — the verification that would have caught it was one `rg` for the new string, which costs nothing.", "rule_affected": ["skills/xreview.md", "skills/tdd-discipline.md"], "resolved_by": "scripts/calibrate.ts"}
{"id": "sig-0039", "ts": "2026-08-09T19:25:00-03:00", "type": "the-honour-system-hole-came-back-wearing-a-hash", "phase": "verify", "severity": "high", "detail": "Measured an hour apart on the same lens and the same fixtures: `--runs 10` caught a flip on race-good and returned `failed`; `--runs 2` returned 10/10 clean and `passed`. Both receipts genuine, both carrying true hashes of the real prompt and the real fixture set. The morning's fix replaced a hand-typed `status: passed` with a measurement — and left the SIZE of that measurement unconstrained, so `--runs 1` produces a real receipt that grants blocking authority on a sample too small to catch anything. The same hole, one level deeper, in the code written to close it. Found by accident: the cheap 2-run sweep I ran to test the plumbing OVERWROTE the honest failing receipt with a passing one, which is precisely how it would happen to a user. Fixed with `MIN_AUTHORISING_RUNS = 10` checked in `blockAuthority`, not in `recordCalibration` — the small run is still recorded honestly because diagnosing is what `--runs 2` is for; it simply authorises nothing. Verified across 1, 2, 5, 9, 10, 20. Lesson: when you replace a claim with a measurement, the measurement's own parameters become the new place to lie. Ask what the cheapest passing measurement looks like, because that is the one that will be run.", "rule_affected": ["skills/xreview.md"], "resolved_by": "src/core/calibration/receipt.ts@MIN_AUTHORISING_RUNS"}
{"id": "sig-0040", "ts": "2026-08-09T20:05:00-03:00", "type": "flag-declared-but-never-plumbed", "phase": "dispatch", "severity": "medium", "detail": "Added `--prepare` to `wst run`: declared the option in `cli.ts`, implemented the branch in `commands/run.ts`, typechecked clean, 570 tests green. The flag did nothing. `cli.ts`'s run action maps options EXPLICITLY into the command's input object, one `...(opts.x !== undefined ? {x: opts.x} : {})` per flag, and I added the option and the implementation without adding the line between them. Nothing catches this: the option parses, the type of the untouched literal is still valid, and no test covers `src/commands/`, which `.sdd/triage.yaml` puts at `light` tier precisely because it should hold no decisions. The cost was not the bug, it was the verification: the live run dispatched a real crewmate and ran the agent-lens for 42 seconds before I saw `--prepare` had been ignored. Second time today a change was reported working without confirming the edit reached the running path (sig-0038 was a silent str.replace). Lesson: for a flag, the wiring is three separate places and the type system checks none of the joins. Verify a new flag by running it and reading the FIRST line of output, not the last — the first line is where 'it did the old thing' shows up, and by the last line you have already paid for it.", "rule_affected": ["skills/xreview.md"], "resolved_by": "src/cli.ts"}
{"id": "sig-0041", "ts": "2026-08-10T00:40:00-03:00", "type": "definitions-only-is-second-class-in-dispatch", "phase": "dispatch", "severity": "high", "detail": "First install into a repo Whetstone did not grow up in (a payments API with its own 412-line CLAUDE.md and a plugin owning .claude/). `--definitions-only` is the mode written for exactly that — its doc comment in commands/init.ts names that case — but `wst run` assumes the emitter ran. core/dispatch/charter.ts hardcodes a 'Read before you write' list containing AGENTS.md (never written in this mode) and .sdd/architecture.md (never written by init at all, and the charter even describes its FCIS content, which is a fact about Whetstone, not the target). Worse, commands/run.ts:66 and :118 pass `strictPaths: [\"src/core/\", \".sdd/skills/\", \".claude/hooks/\"]` as a LITERAL rather than deriving them from the triage rules already loaded two lines earlier for gatingChecks. A crewmate dispatched into the backend would be told three directories it will never touch are the dangerous ones, and told nothing about migrations/ — where this repo's worst production incident actually happened. The charter's own line, 'a gate you cannot see is a trap, not a standard', is the argument: a charter naming the WRONG paths is worse than one naming none, because it reads as authoritative. Lesson: a coexistence mode is not finished when init respects it; every downstream consumer of what init would have written has to respect it too.", "rule_affected": ["skills/delegation.md"]}
{"id": "sig-0042", "ts": "2026-08-10T00:45:00-03:00", "type": "init-emits-text-calibrated-for-the-mode-it-did-not-run", "phase": "init", "severity": "medium", "detail": "Two artifacts written by `wst init --definitions-only` describe a world where the emitter ran. (1) It still copies eight skills into .sdd/skills/. Tracing consumers: core/init/plan.ts:101 (the emitter, which does not run in this mode) and commands/retro.ts:105 (which lists them for amendment). Nothing surfaces them to an agent, so they are structurally unreachable — not merely redundant with the host plugin's calibrated versions, but dead files a reader will assume are live. In this repo the host's own TDD skill carried domain-specific money rules and its own review skill carried a data-boundary gate; the generic copies say neither, and nothing tells a reader which of the two is inert. (2) The generated `light` triage row asserts 'AGENTS.md and CLAUDE.md are generated from .sdd/ — edit the source and regenerate rather than patching them here'. Under --definitions-only both halves are false, and it is an INSTRUCTION: an agent will follow it, decline to edit the host's hand-maintained CLAUDE.md, and go hunting for a source that does not exist. Both were fixed by hand after the write. Lesson: generated prose that describes the tool's own behaviour has to be generated per mode, or it becomes the most confident wrong thing in the repo.", "rule_affected": ["skills/doc-locations.md"]}
{"id": "sig-0043", "ts": "2026-08-10T00:50:00-03:00", "type": "seeded-checks-inherit-scripts-unread", "phase": "init", "severity": "high", "detail": "init wires whatever package.json declares, without reading what the script does. Two of three checks were wrong here. (1) `lint` was seeded as `npm run lint`, which in this repo is `eslint --fix './src/**/*.ts'` — a check that rewrites the tree while judging it. It reports on a file that no longer exists in the form the author wrote it, and hides the finding it exists to surface. The host CLAUDE.md warns about this in prose; init could not read the prose, but it could have read the `--fix`. (2) `test` was seeded at severity block on the evidence that a test script and test files exist. Large parts of this suite open real connections to Postgres on localhost:5432 — with the DB down, 33 tests fail for reasons unrelated to any diff, so the check is red on every machine without `npm run db:start`. That is precisely the routed-around-gate failure that init's OWN seeded lint.md argues against, one check over, in the same directory. Fixed by hand: lint command to `npx eslint` without autofix, test down to warn with the reason written in. Lesson: the existence of a script is not evidence of what it does or whether it passes. Grep the command for mutating flags, and either run the suite once during init or seed brownfield repos at warn until a first green gate earns the promotion.", "rule_affected": ["skills/tdd-discipline.md"]}
{"id": "sig-0044", "ts": "2026-08-10T00:55:00-03:00", "type": "plugin-silence-is-indistinguishable-from-working", "phase": "verify", "severity": "high", "detail": "plugin/hooks/gate-on-stop.mjs:31-33 exits silently on every failure to run: 'No .sdd/, no wst on PATH, not a git repo: none of those are facts about the user.' The reasoning is right and the behaviour should stay. The cost is that there is no way to find out it is happening, and today it happened twice for different reasons with zero indication. (1) The orchestrating session's CLAUDE_PROJECT_DIR was an umbrella folder, which is not a git repo and has no .sdd/ — both hooks inert all session. (2) The dispatched crewmate ran in a treehouse worktree, which IS a repo, but .sdd/ was still untracked and untracked files do not propagate to worktrees. So gate-on-stop never fired for the one agent it most exists to gate, and strict-path-guard never warned on a strict-tier edit to a money path. Point 2 generalises badly: AN UNCOMMITTED .sdd/ SILENTLY DISABLES THE PLUGIN IN EVERY WORKTREE, which is exactly the posture of anyone trialling Whetstone before proposing it to their team — the obvious way to trial it. Suggested fix: `wst status` already computes all of this; give the plugin a row next to .sdd/, judge and pre-push saying whether the hooks are loaded and whether they would do anything from here. Lesson: fail-silent is correct for a hook and wrong for the status command that exists to explain the hook.", "rule_affected": ["skills/recording.md"]}
{"id": "sig-0045", "ts": "2026-08-10T01:00:00-03:00", "type": "adr-deleted-the-code-not-the-documentation", "phase": "review", "severity": "medium", "detail": "Asked whether Whetstone still had a GitHub client. It does not: package.json is commander, yaml, zod and nothing else; commands/pr.ts, core/annotate/ and shell/github.ts are gone, deleted yesterday by ADR-0009 (afe1c8d) with a thorough rationale. For the record the deleted client shelled out to `gh api` via execFile — no HTTP library was ever involved. But .sdd/architecture.md still contradicts itself about it: line 71 correctly strikes it through and cites ADR-0009, while line 22 still lists 'PR criticality annotation' as a live LLM responsibility, line 27's table cell repeats it, and line 127's roadmap still shows '4 annotated PR' as a stage ahead. That file is one of exactly two the crewmate charter orders every dispatched agent to read, so every crewmate reads a self-contradicting description of a subsystem that no longer exists — and one of the four mentions tells it the feature is still coming. Lesson: an ADR that deletes a subsystem has to list the documents that describe it as part of the change, not just the files that implement it. The rules_affected field exists for rules; there is no equivalent pointer for prose, and prose is what the charter makes agents read.", "rule_affected": ["skills/recording.md", "skills/doc-locations.md"]}
{"id":"sig-82dec46b","ts":"2026-08-13T03:39:24.988Z","type":"gate-broken","phase":"other","severity":"medium","detail":"GIT_DIR heredado se filtra a los subprocesos de wst: createGitAdapter(cwd) no lo limpia, el indice del repo principal quedo escrito con el estado de otro worktree y core.bare paso a true dos veces","branch":"main","rule_affected":[],"source":"cli"}
{"id":"sig-672d598d","ts":"2026-08-14T01:40:15.242Z","type":"comment-bloat","phase":"review","severity":"medium","detail":"El codebase tiene 4391 lineas de comentario contra 12448 de codigo (ratio 0,35) y 37 bloques de mas de 15 lineas, el mayor de 42 en core/calibration/receipt.ts. Observado por el humano leyendo el diff de los PRs 21-26. Los que se ganan el espacio nombran evidencia (una medicion, un sig-NNNN); los que sobran re-argumentan una decision que ya vive en un ADR","branch":"fix/destructive-spawns-verify-target","rule_affected":["skills/voice.md"],"source":"cli"}
{"id":"sig-4b3339fb","ts":"2026-08-14T01:40:15.836Z","type":"status-false-negative","phase":"verify","severity":"high","detail":"wst status reporta 'pre-push NOT active' cuando el hook si esta armado: hooksArmed compara configuredPath contra el literal '.githooks' y core.hooksPath quedo con la ruta absoluta. El warning llega a sugerir que armar Whetstone desactivaria otra herramienta, cuando la configurada ES Whetstone","branch":"fix/destructive-spawns-verify-target","rule_affected":[],"source":"cli"}
{"id":"sig-cb978aef","ts":"2026-08-14T01:40:16.352Z","type":"signal-capture-friction","phase":"review","severity":"medium","detail":"wst signal solo se puede tipear en una terminal, y el momento de la friccion es mientras se trabaja en la sesion. El plugin no captura observaciones; ademas el CLI no puede probar que lo tipeo un humano y estampa source=cli. Un comando del plugin tendria mejor procedencia que el terminal","branch":"fix/destructive-spawns-verify-target","rule_affected":[],"source":"cli"}
{"id":"sig-a9ff00c4","ts":"2026-08-14T04:01:56.778Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`docs-fresh` (v1) blocked a change over origin/main..HEAD: > whetstone@0.4.0-alpha check:docs\n> tsx scripts/check-docs-fresh.ts\n\n\nAGENTS.md status line:\n  ## Status — branch `main` · 16 ADRs · 49 signals · 10 commands\n\n  claims 16 ADRs — the repo has 17\n\nUpdate the status line. Every number in it is one command away.","branch":"docs/adr-0017-one-page","source":"gate","fingerprint":"gate-blocked:docs-fresh:1","rule_affected":[]}
{"id":"sig-e8dfefd0","ts":"2026-08-14T04:11:58.453Z","type":"tdd-ceremony-friction","phase":"review","severity":"high","detail":"The repo owner has said three times that separate RED and GREEN commits are unwanted, and the rule kept bringing them back: tdd-discipline [TD1]/[TD2] read 'Commit at RED' / 'Commit at GREEN', a review agent flagged their absence as a hard-rule-4 violation, and an assistant reintroduced them. RED first is not in question; the two-commit ceremony is. The rule as written is what produces the recurrence, so the rule changes: one commit per coherent change, with the red output in the commit body as evidence.","branch":"docs/one-commit-per-change","rule_affected":["skills/tdd-discipline.md"],"source":"cli"}
{"id":"sig-6f2d2b95","ts":"2026-08-15T17:03:02.858Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`test` (v2) blocked a change over main..HEAD: …own system error -122, write\n ❯ installFakeBin test/fake-bin.ts:91:3\n     89|   const dir = await mkdtemp(join(tmpdir(), \"wst-fake-bin-\"));\n     90|   const bin = join(dir, name);\n     91|   await writeFile(bin, SCRIPT, \"utf-8\");\n       |   ^\n     92|   await chmod(bin, 0o755);\n     93|   await wri…","branch":"chore/drop-woz-and-proposals","source":"gate","fingerprint":"gate-blocked:test:2","rule_affected":[]}
{"id":"sig-5c2d6751","ts":"2026-08-15T17:07:22.561Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`docs-fresh` (v3) blocked a change over main..HEAD: > whetstone@0.4.0-alpha check:docs\n> tsx scripts/check-docs-fresh.ts\n\n\nAGENTS.md status line:\n  ## Status — branch `main` · 20 ADRs · 51 signals · 10 commands\n\n  claims 51 signals — the repo has 52\n\nUpdate the status line. Every number in it is one command away.","branch":"chore/drop-woz-and-proposals","source":"gate","fingerprint":"gate-blocked:docs-fresh:3","rule_affected":[]}
{"id":"sig-6406e533","ts":"2026-08-17T00:53:02.660Z","type":"cli-silent-while-working","phase":"verify","severity":"medium","detail":"`wst gate` runs typecheck, test and lint in sequence and prints nothing until every one has finished. Measured in a real foreign repo: typecheck ~3.3s, test 6.7-19s, lint ~2.8s, and an e2e check starts two dev servers first. A 25s run is indistinguishable from a hang, and a Ctrl-C leaves half-written receipts. Same for `prepare` (fetch + worktree) and `init --propose` (a model call announcing only its cost at the end). Needs incremental output: print each check as it starts, update the line with the result and its duration. Constraint: the pre-push hook captures this output and --json must stay clean, so detect a TTY and degrade to flat lines.","branch":"main","rule_affected":["skills/voice.md"],"source":"cli"}
{"id":"sig-4a2610fb","ts":"2026-08-21T00:12:11.342Z","type":"rule-ignored","phase":"other","severity":"medium","detail":"comments must be brief and only where the code cannot be clear; stated twice, applied by hand in PR #80 (36%→30%), and regressed to 33% in PR #87 two days later — nothing holds it","branch":"refactor/the-minimal-whetstone","rule_affected":[],"source":"human"}
{"id":"sig-ea119c62","ts":"2026-08-21T11:49:41.906Z","type":"rule-ignored","phase":"other","severity":"high","detail":"ran git checkout on a file holding an hour of uncommitted work and lost it; hard rule 10 says a deliberate break must be the only uncommitted change, and nothing enforces it — no gate can, because the work is gone before any diff exists","branch":"main","rule_affected":["skills/xreview.md"],"source":"cli"}
{"id":"sig-9ef87566","ts":"2026-08-22T18:53:21.092Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`comment-density` (v1) blocked a change over 52ba44b7f507f90cf14694248cb4076fd5a5594f..d89311a4f804bc946dc111ad88be4f15a105a92f: > whetstone@0.5.0-alpha check:comments\n> tsx scripts/check-comment-density.ts\n\n\n31% of 67 added .ts lines over 52ba44b7f507f90cf14694248cb4076fd5a5594f..d89311a4f804bc946dc111ad88be4f15a105a92f are comment, over the 25% ceiling.\n\nComments belong where the code cannot be made clear on its own.\nHistor…","branch":"fix/retry-what-is-transient","source":"gate","fingerprint":"gate-blocked:comment-density:1","rule_affected":[]}
{"id":"sig-bc543202","ts":"2026-08-22T18:55:47.712Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`docs-fresh` (v4) blocked a change over 52ba44b7f507f90cf14694248cb4076fd5a5594f..11d2d5642329084d14ea05b2b8659546877aae5a: > whetstone@0.5.0-alpha check:docs\n> tsx scripts/check-docs-fresh.ts\n\n\nAGENTS.md status line:\n  ## Status — branch `main` · 26 ADRs · 56 signals · 8 commands\n\n  claims 56 signals — the repo has 57\n\nUpdate the status line, or run `npm run fix:docs`.","branch":"fix/retry-what-is-transient","source":"gate","fingerprint":"gate-blocked:docs-fresh:4","rule_affected":[]}
{"id":"sig-3f92aae7","ts":"2026-08-25T16:23:31.276Z","type":"calibration-passed","phase":"verify","severity":"high","detail":"La lente correctness midió 100/100, unánime en los diez   fixtures, cero errores de harness, 3,36 USD, claude 2.1.241. Los dos fallos de   infraestructura del 2026-08-20 no se reprodujeron y siguen sin diagnosticar.","branch":"main","rule_affected":[],"source":"cli"}
{"id":"sig-b828c2b1","ts":"2026-08-25T19:20:57.697Z","type":"calibration-deadlock","phase":"verify","severity":"high","detail":"Una lente en severity: block no puede re-medirse:   calibrate carga por el mismo registro que rechaza lentes bloqueantes sin receipt   vigente, y el error recomienda re-medir, que es lo que impide. Rodeo a mano: bajar   a warn, medir, subir. Apareció al invalidarse el receipt por un pase de em-dashes.","branch":"feat/the-lens-earns-its-block","rule_affected":["checks/correctness.md"],"source":"human"}
{"id":"sig-39f4aa1e","ts":"2026-08-25T17:00:40.166Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`typecheck` (v2) blocked a change over HEAD: > @juanmzz/whetstone@0.5.0-alpha typecheck\n> tsc --noEmit\n\nsrc/core/gate/report.test.ts(465,76): error TS2353: Object literal may only specify known properties, and 'kind' does not exist in type '{ readonly status: \"errored\"; readonly detail: string; }'.","branch":"fix/init-names-what-it-actually-wrote","source":"gate","fingerprint":"gate-blocked:typecheck:2","rule_affected":[]}
{"id":"sig-575c1b99","ts":"2026-08-29T00:40:09.321Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`test` (v4) blocked a change over origin/main..HEAD: …0m\u001b[38;5;249ms\u001b[0m\u001b[38;5;249m \u001b[0m\u001b[38;5;249mo\u001b[0m\u001b[38;5;249mn\u001b[0m\u001b[38;5;249m \u001b[0m\u001b[38;5;249ma\u001b[0m\u001b[38;5;249m \u001b[0m\u001b[38;5;249mr\u001b[0m\u001b[38;5;249me\u001b[0m\u001b[38;5;249mp\u001b[0m\u001b[38;5;249mo\u001b[0m\u001b[38;5;249m \u001b[0m\u001b[38;5;249mw\u001b[0m\u001b[38;5;249mi\u001b[0m\u001b[38;5;249mt\u001b[0m\u001b[38;5;249mh\u001b[0m\u001b[38;5;249m \u001b[0m\u001b[38;5;249mn\u001b[0m\u001b[38;5;24…","branch":"feat/a-signal-carries-your-own-words","source":"gate","fingerprint":"gate-blocked:test:4","rule_affected":[]}
{"id":"sig-afa2baff","ts":"2026-08-29T04:55:00.255Z","type":"gate-blocked","phase":"verify","severity":"medium","detail":"`evidence-launcher` (v1) blocked a change over 08516b7d4c097ea179d873d58ea431a795294475..b3a07343181674b6127492c5d3a72c96aa9b1748: > @juanmzz/whetstone@0.6.0 check:evidence\n> tsx scripts/check-evidence.ts evidence-launcher\n\n\nevidence-launcher: this change owes evidence of the result, and there is none.\n\nPut it in:\n  /home/juan/playground/proyectos/.wst-evidence/whetstone/feat-the-repo-answers-what-it-declares/evidence-launcher\n…","branch":"feat/the-repo-answers-what-it-declares","source":"gate","fingerprint":"gate-blocked:evidence-launcher:1","rule_affected":[]}
